library plato.angular.models.course.rejected;

import 'course.dart';

/// The [RejectedCourse] class...
class RejectedCourse extends Course {
  /// The [RejectedCourse] constructor...
  RejectedCourse (String id, String title) : super (id, title);
}
